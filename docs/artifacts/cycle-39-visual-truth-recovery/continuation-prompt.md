# Cycle 39 — Continuation Prompt

date: 2026-05-16

Use this prompt to resume Cycle 39 work in a fresh session (e.g., if Mia
review surfaces new defects, or if a follow-up cycle is needed to chase
the daytime-hero swap or live-mode Bridge activation).

## State snapshot

```yaml
branch: main
HEAD_at_cycle_39_close: 889b2c2b117c9bc5fd5bcfc8b97f82e21bae0978
origin_main_at_cycle_39_close: 889b2c2b117c9bc5fd5bcfc8b97f82e21bae0978
working_tree_state: clean except for pre-existing Cycle-35 untracked logs
                    and reports/*.json|md auto-regenerated by audit runs
completed_phases: 0–17 in spirit; live-after capture + final deploy
                  alignment may need a second pass if a docs-only commit
                  lands after the first deploy
incomplete_phases: none required; deferred items recorded below
deferred_items:
  - "Operator decision: keep Cycle 38 twilight hero composition OR swap to
     actual visible miasanabria.com daytime composition (see
     reference-hero-visual-extraction.md). No AI action without sign-off."
  - "Operator decision: flip Bridge from demo → live mode by updating
     Dokploy build args (NEXT_PUBLIC_BRIDGE_DEMO=false + live dataset ID
     + live resource path). External to AI scope; recorded in
     bridge-truthfulness-report.md."
  - "Mia visual approval of Cycle 39 staging hero + neighborhood images.
     External to AI scope."
  - "Production cutover (DNS, GHL endpoints, branded email). Separate
     mission; not Cycle 39."
```

## Resume prompt

```text
Continue Cycle 39 follow-up work in /home/torrey/code/mia-sanabria-website.

Read these before acting:
  docs/artifacts/cycle-39-visual-truth-recovery/staging-live-verification-report.md
  docs/artifacts/cycle-39-visual-truth-recovery/red-team-final-review.md
  docs/artifacts/cycle-39-visual-truth-recovery/reference-hero-visual-extraction.md
  docs/artifacts/cycle-39-visual-truth-recovery/continuation-prompt.md
  docs/artifacts/cycle-39-visual-truth-recovery/rollback-plan.md

Then:
  1. git status; git log --oneline -10; git rev-parse HEAD; git rev-parse origin/main
  2. tmux list-sessions
  3. ps -ef | grep -E "deploy-and-verify|capture-baseline|playwright|google-chrome" | grep -v grep
  4. State the first new defect Mia or operator surfaced and the smallest
     mission that addresses it.

Honor:
  - the bun-always rule (never npm/npx)
  - the no-production rule (DNS/GHL/branded email/Google credential
    rotation are operator-only)
  - the secret-handling rule (no token values to source, logs, transcripts,
    or commits)
  - the locked H1 / hero copy per docs/mia-client-decision-record.md
    §Homepage hero
  - the no-old-IDX rule (audit:no-old-idx must stay PASS)

Use scripts/test-home-search-bridge-e2e.ts and audit:neighborhood-images-deep
in any verification step.
```

## Files most likely to need edit on resume

- `src/lib/markets.ts` — if the daytime-hero swap is approved and additional
  Mia-approved compositions are added.
- `public/hero/mia-home-hero-cycle39.jpg` — swap-in the daytime composition
  byte-for-byte (NO new path) when approved.
- `src/lib/bridge-client.ts` — only on Bridge live activation; coordinate
  with Dokploy build args.
- `src/components/Hero.tsx` — only if mobile review still surfaces a
  visual regression after Cycle 39's panel-opacity + sub-text-width fix.

## Tmux & log artifacts in this cycle

- `mia-c39-live-before-20260516-095902` — live-before capture (DONE).
- `mia-c39-ref-hero` — reference-hero probe (DONE).
- `mia-cycle39-staging-deploy-<ts>` — staging deploy (started Phase 11).

## What success looks like next session

Single most likely next mission:

> "Mia approved the staging hero. Cut over the production DNS for
> miasanabriarealtor.com from Direct Axess to the Dokploy app, validate
> live, smoke-test all key pages, set up monitoring."

That is the operator-led production-cutover mission. It is NOT
AI-closeable today; do not run it in a Cycle 39 follow-up.
