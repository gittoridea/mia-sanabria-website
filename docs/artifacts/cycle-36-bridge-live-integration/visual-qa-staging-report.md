# Visual QA — Staging (post-deploy)

**Generated:** 2026-05-15
**Capture base:** `https://miasanabriarealtor.trueidea.com`
**Capture tool:** `scripts/capture-baseline.ts` (`google-chrome --headless=new`)
**Output dir:** `docs/artifacts/cycle-36-bridge-live-integration/visual-qa/staging/`
**Summary:** `docs/artifacts/cycle-36-bridge-live-integration/visual-qa/staging/_capture-summary.json`
**Log:** `docs/artifacts/cycle-36-bridge-live-integration/logs/staging-visual-qa.log`

## Capture results

```yaml
base: https://miasanabriarealtor.trueidea.com
routes: 36                  # full route inventory across the public site
viewports: 3                # 375x812 (iPhone SE-ish), 768x1024 (iPad portrait), 1280x800 (desktop)
jobs: 108
ok: 108
fail: 0
duration_seconds: 79
```

## Routes captured (each × 3 viewports)

The capture iterated the full route inventory generated by `scripts/capture-baseline.ts` ROUTES export, which covers `/`, `/home-search/`, hub pages (`/markets/`, `/insights/`), all 23 `/markets/<slug>/` market pages, `/buyers/`, `/sellers/`, `/about/`, `/contact/`, `/valuation/`, `/insights/<slug>/` articles, `/privacy/`, `/terms/`, `/accessibility/`, `/dmca/`, and `/404`.

## Visual checks (Cycle 36D focus)

| Question | Answer | Source |
|---|---|---|
| Did all 108 staging captures succeed? | YES (0 failed) | `_capture-summary.json` |
| Did the deploy actually render new content? | YES — etag flipped on `/` (`diijwdedso3k1hiy` → `dijka7eh7g1s57rf-gzip`), `last-modified` advanced to 2026-05-15 21:17:43Z | post-deploy HEAD responses |
| Does staging /home-search/ render the Bridge search form? | YES — form, city dropdown (Fort Lauderdale × 24, Pompano × 6, Deerfield × 2 in HTML), min-price, bedroom selectors | `staging-html/final/_home-search_.html` |
| Does staging /home-search/ reflect demo mode honestly? | YES — initial SSR HTML shows the form; demo banner + DEMO pill render client-side only after a search executes (gated on `searched && listings.length > 0`). This matches chunk behavior since Cycle 33B. | `bridge-staging-final-report.md` |
| Are demo warning / DEMO badges visible on staging without a search? | NOT in initial render. Per design — they appear after the client-side fetch returns demo rows. | initial HTML inspection |
| Is IDX/MLS disclosure visible on staging? | Gated to non-demo mode in chunk. Currently NOT rendered because demo mode is on. | chunk needle audit |
| Any token-like strings rendered in the captured HTML? | NO — live HTML secret scan over 18 fetched HTMLs returned 0 matches | `secret-safety-report.md` |
| Mobile (375x812) regressions across the live site? | NONE — all 36 routes render their hero + nav + footer trust strip cleanly | all `*__375x812.png` |
| Tablet (768x1024) regressions across the live site? | NONE — including the previously-failing Seven Isles + Pompano routes | all `*__768x1024.png` |
| Desktop (1280x800) regressions? | NONE | all `*__1280x800.png` |
| Did staging mobile-readability pass? | YES — 84 PASS · 0 FAIL · 0 ERROR against the live URL | `bun run scripts/audit-mobile-readability.ts --base=https://miasanabriarealtor.trueidea.com` |

## Cross-reference

- Local visual QA: 39 screenshots, 0 fail (`visual-qa-local-report.md`)
- Staging visual QA: 108 screenshots, 0 fail (this report)
- Mobile readability local: 84/0/0
- Mobile readability staging: 84/0/0

## Notes

- The capture script writes one PNG per route × viewport into `visual-qa/staging/`. Per `.gitignore:55-56`, the PNGs are not committed; the `_capture-summary.json` is the durable record.
- Final pixel-level pass/fail is enforced by `audit:hero-contrast:stable` against the locally-exported `out/` (which Dokploy serves verbatim under Caddy), not by these screenshots. The 108-shot capture against the live URL is the human-eyeball cross-check.
