# Cycle 26 — Cycle 25 Commit Bloat / Artifact Review

**Generated:** 2026-05-13.
**Subject:** Commit `e32310d` — 56 files changed, +17,187 / −2,897.

## Method

`git show --stat`, `git show --name-status`, and `git show --numstat` on the Cycle 25 commit. Each changed file classified into one of nine categories per the Cycle 26 mission brief.

## Classification table

| Category | Count | Files | Action |
|---|---|---|---|
| source code intentionally changed | 3 | `src/lib/markets.ts` (+529), `src/lib/mia.ts` (+29), `scripts/render-images.ts` (+114 / −11) | KEEP — the cycle's deliverable surface |
| public assets intentionally added | 14 | `public/markets/{deerfield-beach,coral-springs,plantation,weston,hollywood,davie,sunrise}.jpg` + `public/og-markets/{same 7}.jpg` | KEEP — the cycle's image-asset deliverable; each 36–66 KB, sharp-rendered from inline SVG |
| docs/artifacts intentionally added | 17 | 8 agent memos + 7 city briefs + `page-model.md` + `site-continuity-table.md` under `docs/artifacts/cycle-25-neighborhood-content/` | KEEP — audit evidence for Cycle 25 decisions; verbose by design |
| reports regenerated by audit:all | 22 | 22 files under `reports/` (`audit-about`, `audit-brand-consistency`, `audit-completeness`, `audit-featured-markets`, `audit-fort-lauderdale-standard`, `audit-hero-pixel-contrast`, `audit-images`, `audit-insights`, `audit-legal`, `audit-rendered-visual`, `qa-gate-matrix` × {.json,.md}) | KEEP — established pattern since at least Cycle 19C; each prior cycle also commits regenerated audit reports |
| suspicious generated output | 0 | — | none found |
| accidental bloat | 0 | — | none found |
| stale duplicate docs | 0 | — | none found |
| safe to remove now | 0 | — | none found |
| defer / keep for audit trail | 1 | `reports/audit-rendered-visual.json` is now 50,153 lines / 1.4 MB after a +16,936-line growth in Cycle 25 | DEFER — addressing this would change tooling pattern across all prior cycles; not in Cycle 26 scope |

## Findings

### 1. `reports/audit-rendered-visual.json` size growth

The file added 16,936 lines in Cycle 25 — by far the largest single-file growth in the commit. It is now 50,153 lines / 1.4 MB. Cause: the audit emits one row per (route × viewport × probe) tuple; adding 7 new market routes grows the matrix by 7 × N where N is the number of viewports × probe-types.

This file has been tracked in every cycle since at least Cycle 19C. Changing its tracking strategy (e.g., compressing to a summary, moving to gitignore, or pruning historical detail) would:

- Invalidate the established audit-evidence pattern, which other reviewers may rely on.
- Be a tooling cycle of its own — out of Cycle 26 surgical-QA scope.

**Recommendation:** queue a future tooling cycle that decides whether `audit-rendered-visual.json` should be summarized in git and kept full-detail in a side path (or simply gitignored). Not addressed here.

### 2. No accidental generated output

- `out/` is gitignored (`.gitignore:13`) — confirmed not tracked.
- `.next/` is gitignored (`.gitignore:12`) — confirmed not tracked.
- `docs/artifacts/**/*.jpg` / `docs/artifacts/**/*.png` are gitignored (`.gitignore:41-42`) — captured screenshots in any cycle artifact dir are not tracked, which is the desired behavior. Reproducible via `audit:mobile-readability:capture`.
- `node_modules/`, `coverage/`, `build/`, `dist/`, `audit-reports/`, `audits/`, `screenshots/`, `.tmp/`, `tsconfig.tsbuildinfo` — all gitignored.

### 3. No stale duplicate docs

The 17 docs added in Cycle 25 are unique artifacts (8 agent memos + 7 city briefs + page-model + site-continuity-table). No duplicates of prior cycle docs.

### 4. No misclassified files

Each binary `.jpg` in the diff is a Cycle-25 placeholder hero or OG card with `Bin 0 -> NNNNN bytes` — i.e., a new file, not a modification. No existing image was clobbered in the commit (the pre-commit revert of the unintentional rerenders is visible in the closeout block of the Cycle 25 report).

## Conclusion

**Zero accidental bloat in commit `e32310d`.** The size impact is concentrated in `audit-rendered-visual.json` (predictable +N route growth) and the 17 narrative docs (intentional Cycle 25 audit evidence per the multi-agent mission brief). No corrective action needed in Cycle 26; one follow-up flagged for future tooling consideration.
